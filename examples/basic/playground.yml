servers:
  - name: playground
    routes:
      - path: /
        dir: examples/basic/public
    playground:
      html: |
        <div class="main">
            Hello world!
        </div>
      css: |
        @import url("/reset.css");
        :root {
            border: 50px solid pink;
            height: 100vh;
            overflow: hidden;
        }
