servers:
  - port: 3001
    routes:
      - path: /shane
        raw: "here2"
        when:
          query:
            - has: 'abc'
      - path: /shane
        raw: "here"
        when:
          query:
            - has: 'def'
      - path: /shane
        dir: examples/basic/public
